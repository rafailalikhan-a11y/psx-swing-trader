name: psx_swing
description: PSX Swing Trader — portfolio tracker + swing-trade signals for Pakistan Stock Exchange.
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  csv: ^6.0.0
  file_picker: ^10.1.2
  shared_preferences: ^2.2.3
  fl_chart: ^0.69.0
  intl: ^0.19.0
  flutter_local_notifications: ^18.0.1
  workmanager: ^0.10.9

flutter:
  uses-material-design: true
